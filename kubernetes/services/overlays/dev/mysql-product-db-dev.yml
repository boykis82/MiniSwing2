apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-product
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-product
  template:
    metadata:
      labels:
        app: mysql-product
    spec:
      containers:
      - name: mysql-product
        image: mysql:5.7
        volumeMounts:
          - name: product-db-volume
            mountPath: /var/lib/mysql
          - name: mysql-custom
            mountPath: /etc/mysql/conf.d/custom.cnf
        args:
          - "--ignore-db-dir=lost+found"
        env:
          - name: MYSQL_ROOT_PASSWORD
            value: rootpwd
          - name: MYSQL_DATABASE
            value: product
          - name: MYSQL_USER
            value: user
          - name: MYSQL_PASSWORD
            value: pwd
        ports:
          - containerPort: 3306
        resources:
          limits:
            memory: 350Mi
      volumes:
        - name: product-db-volume
          hostPath:
            path: /miniswing/swingdb/product
            type: Directory
        - name: mysql-custom
          hostPath:
            path: /miniswing/mysql-custom.cnf
            type: File
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-product
spec:
  selector:
    app: mysql-product
  ports:
    - port: 3306
