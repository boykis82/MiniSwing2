apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-service
  template:
    metadata:
      labels:
        app: mysql-service
    spec:
      containers:
      - name: mysql-service
        image: mysql:5.7
        volumeMounts:
          - name: service-db-volume
            mountPath: /var/lib/mysql
          - name: mysql-custom
            mountPath: /etc/mysql/conf.d/custom.cnf
        args:
          - "--ignore-db-dir=lost+found"
        env:
          - name: MYSQL_ROOT_PASSWORD
            value: rootpwd
          - name: MYSQL_DATABASE
            value: service
          - name: MYSQL_USER
            value: user
          - name: MYSQL_PASSWORD
            value: pwd
        ports:
          - containerPort: 3306
        resources:
          limits:
            memory: 350Mi
      volumes:
        - name: service-db-volume
          hostPath:
            path: /miniswing/swingdb/service
            type: Directory
        - name: mysql-custom
          hostPath:
            path: /miniswing/mysql-custom.cnf
            type: File
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-service
spec:
  selector:
    app: mysql-service
  ports:
    - port: 3306
