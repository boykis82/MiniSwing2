apiVersion: apps/v1
kind: Deployment
metadata:
  name: plm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: plm
  template:
    metadata:
      labels:
        app: plm
    spec:
      containers:
      - name: plm
        image: mini-swing-pilot/plm-server
        imagePullPolicy: Never
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "docker"
        ports:
        - containerPort: 80
        resources:
          limits:
            memory: 350Mi
        livenessProbe:
          httpGet:
            scheme: HTTP
            path: /actuator/info
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 2
          failureThreshold: 20
          successThreshold: 1
        readinessProbe:
          httpGet:
            scheme: HTTP
            path: /actuator/health
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 2
          failureThreshold: 3
          successThreshold: 1
---
apiVersion: v1
kind: Service
metadata:
  name: plm
spec:
  selector:
    app: plm
  ports:
    - port: 80
      targetPort: 80
